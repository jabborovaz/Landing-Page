import { lazy } from "react";

export const Registration = lazy(() => import("../Container/Registration"));
export const Login = lazy(() => import("../Container/Login"));
export const Layout = lazy(() => import("../Container/Layout"));
